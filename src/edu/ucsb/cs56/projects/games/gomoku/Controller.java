package edu.ucsb.cs56.projects.games.gomoku;

/**
 * a controller to control user interactions, such as clicking button
 * currently there's nothing inside due to the game being incomplete
 * and there is no user interaction yet
 */

public class Controller
{
	/* at first I thought maybe mouseListener should be in this part
	 * but then I realize that the mouseClicked actually change the 
	 * state and therefore should be in model
	 *
	 * 
	 * currently there's nothing inside this class, because there's 
	 * no widget like button or textfield, I don't think the complete
	 * game has been written or merge yet, and therefore this class 
	 * remains empty 
	 */
}
